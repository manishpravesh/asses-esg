# Sources

This repository’s canonical source research documentation lives in:

- `docs/SOURCES.md`

(This root-level file is intentionally kept as a pointer to avoid duplicated or conflicting docs.)
